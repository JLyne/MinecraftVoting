{
    "private": true,
    "scripts": {
        "dev": "vite",
        "preview": "vite preview",
        "build": "vue-tsc --noEmit && vite build",
        "lint": "eslint --ext .ts,.vue src",
        "lint:fix": "eslint -ext .ts,.vue src --fix"
    },
    "dependencies": {
        "modern-normalize": "^1.1.0"
    },
    "devDependencies": {
        "cpy-cli": "^3.1.1",
        "eslint": "^8.8",
        "sass": "^1.49",
        "vite": "2.6.14",
        "vite-plugin-laravel": "^0.2.0-beta.5"
    }
}
